# Tree Traversals

**Proves:** that the three traversals differ by one line's
position, and that the difference is visible in the call tree rather than
something you have to take on faith. The recursion is identical in all three;
only *when* the node is appended changes.

In-order comes out sorted. That is not a coincidence -- it is the BST invariant
restated.
