# Euclid's GCD

**Proves:** that a handful of iterations is enough to make an
argument. Watch `a` and `b` shrink and note how few steps it takes even for
large inputs — the loop-iteration count in the analytics panel *is* the
`log min(a,b)` bound, measured.
