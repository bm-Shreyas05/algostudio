# Counting Sort

**Proves:** an algorithm with *zero* comparisons still produces a
rich visualization — two array views side by side, the input and the tally, with
the analytics panel showing `comparisons` absent entirely.

This is the cleanest demonstration that the visualization is derived from what
the program did, not from a template for "sorting".
