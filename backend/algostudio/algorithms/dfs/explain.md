# Depth-First Search

**Proves:** the same graph view, driven by a stack instead of a
queue. Run BFS and DFS on the identical input and watch the difference in the
order nodes light up -- the only change is which end of the container the
algorithm takes from, and the stack/queue lifter reports each correctly.
