# Insertion Sort

**Proves:** the array view's shift animation, and that "best case
O(n)" is a claim you can *check* — run it on an already-sorted array and watch
the comparison count in the analytics panel collapse to n-1.
