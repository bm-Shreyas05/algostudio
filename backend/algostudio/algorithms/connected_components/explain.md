# Connected Components

**Proves:** a disconnected graph. The default input has two
components (A-B-C-D-E and F-G); watch the outer loop restart the flood when it
reaches F, and the graph view show two islands.
