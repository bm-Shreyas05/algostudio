# Heap Sort

**Proves:** an array being read as a tree. The same list is the
sorted output *and* the heap; switch the view between Array and Heap on the same
object to see both readings of the identical data.
