# Anagram Check

**Proves:** dictionary mutation, including deletion. The table view
shows rows appearing, counting down, and being removed — and stepping backwards
puts each deleted key back in its original position, which is the invertible
`SUBSCRIPT_DELETED` event doing its job.
