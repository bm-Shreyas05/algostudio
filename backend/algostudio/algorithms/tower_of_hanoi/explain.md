# Tower of Hanoi

**Proves:** three stacks mutating in one structure, and a call tree
that is exactly the recursion the classic proof describes. The move count is
2^n - 1 every time -- check it in the analytics panel for a few values of n
rather than believing the formula.
