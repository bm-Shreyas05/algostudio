# Linear Search

**Proves:** the pointer lifter on the simplest possible loop — `i`
becomes a caret under the array purely because it is an integer used as a
subscript index.

Run this and Binary Search on the same 1000-element array and compare
`array_reads` in the analytics panel. That number is the whole argument for
sorting your data first.
