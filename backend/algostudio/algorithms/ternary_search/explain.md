# Ternary Search

**Proves:** that asymptotics can mislead, and that the tool settles
it. Ternary search does fewer *iterations* than binary search but more
*comparisons per iteration*. Run both on the same array and read the actual
comparison count — the measured number is the answer, not the log base.
