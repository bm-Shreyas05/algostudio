# Longest Increasing Subsequence

**Proves:** two arrays shown together, the input and the DP state,
with pointers `i` and `j` on both. The inner loop scanning everything before `i`
is the quadratic cost made visible.
