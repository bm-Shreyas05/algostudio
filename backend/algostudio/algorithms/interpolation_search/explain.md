# Interpolation Search

**Proves:** an algorithm whose performance depends on the *shape*
of the data, not just its size. Run it on the evenly spaced default (it lands
almost immediately), then on a clustered array like `[1,2,3,4,5,6,7,8,9,1000]`
and watch the probe crawl.
