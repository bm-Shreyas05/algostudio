# Radix Sort

**Proves:** stability as an observable property. Step through one
`counting_pass` and watch that equal digits keep their relative order — that is
exactly why the algorithm works when the passes are composed.
