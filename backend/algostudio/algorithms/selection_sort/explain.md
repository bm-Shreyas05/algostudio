# Selection Sort

**Proves:** that comparison count and swap count are independent
axes. Selection sort does the same n(n-1)/2 comparisons on every input but at
most n-1 swaps — compare its analytics against bubble sort on the same array.
