# Rod Cutting

**Proves:** the same table shape as coin change with a maximum
instead of a minimum. Worth running both back to back -- the structure is
identical and only the comparison direction differs, which is the point of
recognising a DP pattern.
