# Prime Factorization

**Proves:** a loop bound that is not obvious from the source.
`divisor` stops at sqrt(remaining), and because `remaining` shrinks as factors
come out, the bound tightens as it goes. Watch both variables together.
