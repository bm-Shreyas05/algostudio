# Jump Search

**Proves:** a search whose cost sits between linear and binary.
The region between `prev` and `current` is the live search window; watch it slide
forward in blocks rather than halving.
