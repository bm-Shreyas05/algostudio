# Rabin-Karp

**Proves:** the difference between a hash match and a real match.
Watch `window` and `target` in the variables panel: `matches()` is only called
when they agree, and the analytics panel's `function_calls` count tells you how
often the hash was right.
