# Palindrome Check

**Proves:** the two-pointer idiom. `low` and `high` both become
carets under the string, converging from the ends — the pointer lifter finds
them without being told they are pointers.
